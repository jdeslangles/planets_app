Head....

<!-- < % @planets.each do |planet| %>
  < %= render partial: 'planet', locals: {planet: planet} %>

This "locals...." bit is allowing us to access local variables within the partial _planet form - which would break otherwise

<!--  better: see below -->
<!--  < %= render planet %>
<!-- < % end %> -->

<!-- Even better: -->

<%= render @planets %>
.....